// Host half of dsh-zen: minimal placeholder. All zen logic lives in the
// browser half (client.js) — pure front-end, no host services needed.
export const name = "dsh-zen";

export function apply(ctx) {
  // reserved: future settings namespace (zen hotkey, auto-hide options)
}
